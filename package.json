{
  "name": "taskpal-backend",
  "version": "1.0.0",
  "description": "Backend for TaskPal application",
  "main": "src/index.js",  // Adjusted if your entry file is inside the src folder
  "scripts": {
    "start": "node index.js", 
    "build": "echo 'No build process required for backend.'",
    "postdeploy": "echo 'Deployment is complete!'",
    "install": "npm install" 
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.9.10"
  },
  "engines": {
    "node": ">=18.18.0 <19.0.0"
  }
}
